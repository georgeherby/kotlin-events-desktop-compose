package theme

import androidx.compose.material.darkColors
import androidx.compose.material.lightColors
import androidx.compose.ui.graphics.Color

object Colours {
    val darkColours = darkColors(primary = Color(245, 127, 127), surface = Color(0xFF121212))
    val lightColours =  lightColors(primary = Color(222, 78, 78), surface = Color.White)
}
